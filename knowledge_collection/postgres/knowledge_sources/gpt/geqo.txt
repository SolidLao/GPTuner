The 'geqo' knob in PostgreSQL is a Boolean type, so it can be set to either 'on' or 'off', with the default being 'on', which enables the Genetic Query Optimization approach for executing complex queries.