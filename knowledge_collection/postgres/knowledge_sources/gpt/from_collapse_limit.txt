The 'from_collapse_limit' knob in Postgres controls the number of FROM items that a subquery can contain before the planner collapses it into a single item; the default value is 8, but it can be increased for complex queries with many JOINs, though it may increase planning time.