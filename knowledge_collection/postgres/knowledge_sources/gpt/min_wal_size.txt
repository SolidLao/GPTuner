The 'min_wal_size' knob in Postgres should be set based on your workload, with a general recommendation being 1GB, but it can be increased if your system has heavy write loads, as it determines the minimum size to shrink the WAL to after old segments are removed.