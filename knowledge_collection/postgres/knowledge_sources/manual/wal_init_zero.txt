The 'wal_init_zero' knob, when turned on (which is the default setting), fills new WAL files with zeroes to pre-allocate space, but this may not be beneficial for Copy-On-Write (COW) file systems, hence it can be turned off to write only the final byte when the file is created.