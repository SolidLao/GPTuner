The 'slave_parallel_type' knob in MySQL can be set to either 'DATABASE' or 'LOGICAL_CLOCK' where 'DATABASE' allows parallelism for transactions using different databases and 'LOGICAL_CLOCK' allows parallelism for any transaction as long as they are not dependent on each other.